import { ThreePointLeaderPipe } from './three-point-leader.pipe';

describe('ThreePointLeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new ThreePointLeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
